# 交互覆盖、i18n 覆盖、选择器重做与 `info` 性能 —— 设计方案

> 上游:`2026-08-22-interaction-theme-and-layout-design.md`(已发布 2026.8.22.3)
> 起因:2026.8.22.3 发布后的真实使用反馈
> 本文覆盖四件事:`info` 慢 2.4 秒、i18n 远未全覆盖、交互覆盖面不足、选择器形态与配色

---

## 0. 结论先行

| | 判定 | 依据(实测) |
|---|---|---|
| §1 `info` 耗时 2.4s | **性能缺陷** | 全部是用户态 CPU;与包大小**无关**(gcc 576 行与 proot 60 行同为 2.38s);热点是遍历 1808 条版本记录做字符串切分 |
| §2 i18n 覆盖 | **远未完成** | 已覆盖 ~40 条;未覆盖 **159 处**(spec.cpp 110 + cli.cpp 49),等于所有子命令的帮助 |
| §3 交互覆盖 | **不足** | 现有 8 处;应扩到 **31 处**(见 §3.3) |
| §4 选择器形态与配色 | **可用性缺陷** | 六处边框全用 `border` 槽,深色下为 `#334155`(slate-700),在深色背景上对比度约 1.6:1 |

需要你裁决的是 §4.2 的形态取舍;其余三项是缺陷或已确认的要求,无需取舍。

---

## 1. `xlings info` 的 2.4 秒

### 1.1 实测

```
xlings config       0.04 s
xlings search gcc   0.06 s     ← 同样加载索引
xlings list gcc     0.82 s
xlings info gcc     2.37 s
```

`real 2.377s / user 2.357s / sys 0.019s` —— **没有 I/O 等待,全是计算**。

且与被查的包**无关**:

| 包 | 配方行数 | 耗时 |
|---|---|---|
| gcc | 576 | 2.38 s |
| proot | 60 | 2.39 s |
| node | — | 2.37 s |
| mcpp-short-cmd | — | 2.38 s |

一个与查询内容无关的**常量开销**。`search` 走同一个 `get_catalog(LocalOnly)` 却只要 0.06s,所以代价不在索引。

### 1.2 热点(gdb 采样)

```
coordinate_from_payload_path        ← std::views::split,每个路径组件构造一个 std::string
  ← shallow_owner_candidates_for
  ← build_owner_coordinates
  ← assemble_inventory
```

规模:这个 home 有 **1312 个包 / 1808 条版本记录**。`info` 为了回答末尾那两行

```
bindings: xim-aarch64-musl-gnu-gcc, xim-gnu-gcc -> 16.1.0, xim-musl-gnu-gcc
subos: default, dev-hello, gfxbuild, mcpp-test
```

装配了**完整清单**,而每条记录都要把 payload 路径切成组件、逐个构造 `std::string`,再从后往前找 `xpkgs`。1808 次 × 每次约 8 个组件 = 约 1.5 万次堆分配加比较,只为得到两个字段(`ns`/`package`/`version`)。

### 1.3 方案

三层,按收益/风险排序:

**A. 让 `coordinate_from_payload_path` 不分配。** 它要的只是"最后一个 `xpkgs` 之后的两段"。用 `string_view` 从右往左扫,零 `std::string` 构造。这是纯局部优化,不改语义,单测可逐字节比对新旧结果。

**B. `info` 不该装配完整清单。** 它要的是"哪些 subos 引用了**这一个** target"。`assemble_inventory` 是为 `list`/`doctor` 那种全量视图写的;`info` 应当走一个按 target 过滤的窄路径 —— 即先筛出该 target 的版本记录(1808 → 通常个位数),再只对它们求 owner。

**C. 缓存 payload→坐标 的映射。** 只在一次进程内多次装配时有收益,`info` 是单次,所以**不做** —— 记在这里是为了说明为什么不做。

**实测结果(A 已实施):**

| 命令 | 改前 | 改后 |
|---|---|---|
| `info gcc` | 2.40 s | **1.26 s** |
| `list gcc` | 0.83 s | 0.46 s |
| `list` | 0.45 s | 0.34 s |

输出**逐字节相同** —— `info gcc` / `info node` / `info proot` / `list gcc` / `list` 五组,去掉 SGR 后 diff 为空。先证明这一点再看计时;反过来无法区分"变快了"和"少做了事"。

**B(已实施):问题不是"算得慢",是"算了不该算的东西"。**

原来的写法对**每个** pair 问一次「你能不能到达匹配 filter 的东西」,于是把整张绑定图走了 1808 遍,每遍各建一个 `std::set` 的 visited。

关键观察:那个遍历是**无向**的 —— 它同时走 `incoming`(谁绑定到我)与 `bindings`(我绑定到谁)。所以「P 能到达匹配的 Q」与「Q 能到达 P」是同一句话,整个答案是**从匹配的节点向外走一遍**,而不是每个节点向内走一遍。

种子的选取不含任何图操作:自身坐标(direct / provider / payload-path 三者之一)匹配 filter 的 pair。`gcc` 的绑定组就是 `xim-gnu-gcc` 那几个,BFS 走几步就停。

| 命令 | 2026.8.22.3 | P1 后 | **P1+B 后** |
|---|---|---|---|
| `info gcc` | 2.37 s | 1.26 s | **0.13 s** |
| `list gcc` | 0.83 s | 0.46 s | **0.11 s** |
| `list` | 0.45 s | 0.34 s | 0.33 s |

输出**七组逐字节相同**(`info` × 4、`list` × 3,去 SGR 后 diff 为空)。

参照系:`search gcc`(同样加载索引)0.06s,`config`(什么都不加载)0.04s。**`info` 现在比"加载索引"只多 0.07s,剩下的已经是索引加载本身,不再是遍历。**

**C(缓存)不做,而且现在有了更强的理由。** 目标 0.5s 已经被 B 以 0.13s 达成。缓存只会把一个**不该存在的计算**存起来,并引入陈旧数据这一整类风险 —— 而这个代码库反复被"报告成功却什么都没做"咬过,缓存返回过期结果正是那个形状。

**订正一处方案里的误判:subos 遍历不是问题。** 45 个 subos 的 `.xlings.json` 合计 312KB,读取是毫秒级,四次采样没有一次落在那里。慢的始终是版本 DB 的 1808 条记录。§1.3 原文暗示过"扫 45 个 subos"是代价之一,那是错的。



### 1.4 验证方式

不能只测 `info gcc` 变快了 —— 那无法区分"变快"和"少做了事"。必须:

1. 同一个 home 上,新旧二进制的 `info` **输出逐字节相同**(含 `bindings` 与 `subos` 两行)
2. `list` / `doctor` / `remove` 三条同样使用 `assemble_inventory` 的路径输出不变
3. 计时在**同一个 home**(1312 包)上做,并记录包数 —— 在空 home 上测会得到一个没有意义的数字

---

## 2. i18n 覆盖

### 2.1 现状:覆盖了顶层,漏掉了全部子命令

2026.8.22.3 翻译的是 `ui/banner.cpp` 里**手写的顶层命令表**。而 `xlings subos -h` 的内容来自 `src/cli/spec.cpp`,那里一条也没接:

```
  xlings subos
  Manage SubOS environments        ← 未翻译

  用法                             ← 已翻译(章节标题)
    xlings subos [SUBCOMMAND]

  子命令                           ← 已翻译
    new     Create a SubOS         ← 未翻译
    use     Enter a SubOS          ← 未翻译
```

章节标题是中文而内容是英文,比全英文更难读 —— 它让人以为翻译坏了。

### 2.2 未覆盖的量(实测)

| 来源 | 条数 | 内容 |
|---|---|---|
| `src/cli/spec.cpp` | **110** | 全部子命令、参数、选项的描述 |
| `src/cli.cpp` 的 `.description()` / `.help()` | **49** | 同上,注册时的另一份 |
| `src/ui/*.cpp` 字面量 | 4 | 面板表头等 |
| **合计** | **163** | |

已覆盖约 40 条。**目标不小于 100 处**是合理的 —— 单是 spec.cpp 的 110 条就超过了,不需要为了凑数去翻译不该翻译的东西。

### 2.3 一个必须先解决的结构问题

`spec.cpp` 与 `cli.cpp` **各存了一份命令描述**,而且它们必须一致(CI 有一条 "cli spec parity" 检查在盯)。翻译时若只改一处,parity 检查会红;两处都改则等于把同一段文案维护两遍。

所以顺序是:**先合并成一处**,再接 i18n。合并方向是 `spec.cpp` 作为唯一来源,`cli.cpp` 注册时从它取描述 —— spec 已经是被 parity 检查认定的权威。

### 2.4 覆盖范围与判据

判据不变:**能敲进终端的东西不翻,解释它的话翻。**

| 类别 | 数量 | 翻译? | 说明 |
|---|---|---|---|
| 章节标题(`USAGE`/`SUBCOMMANDS`/`OPTIONS`/`ARGS`) | 5 | ✅ 已完成 | |
| 顶层命令描述 | 12 | ✅ 已完成 | |
| 全局选项描述 | 4 | ✅ 已完成 | |
| `config` 字段标签 | 11 | ✅ 已完成 | |
| **子命令描述**(`subos new` / `use` / `list` …) | ~35 | ❌ 待做 | §2.2 的主体 |
| **子命令选项描述**(`--storage` / `--sandbox` …) | ~55 | ❌ 待做 | |
| **位置参数描述**(`SubOS name` / `Package names` …) | ~20 | ❌ 待做 | |
| **交互提示语**(`Which theme ?` / `Select a package:`) | 8 | ❌ 待做 | 见 §3;**交互本身也要 i18n** |
| **面板表头**(`Sub-OS environments:` / `Installed packages`) | ~12 | ❌ 待做 | `subos use` 的列表就在这里 |
| 命令名、旗标名、占位符、环境变量名 | — | ❌ 永不翻译 | `install` / `--yes` / `<version>` / `XLINGS_HOME` |
| `std::format` 的参数(版本号/路径/包名) | — | ❌ 永不翻译 | 翻译数据会让复制出来的命令失效 |
| `log::debug`(106 处) | 106 | ❌ 不翻译 | 读者是开发者与 issue 报告,英文更利检索 |

待做合计 **约 130 处**。

---

## 3. 交互覆盖

### 3.1 现状:8 处

| # | 位置 | 状态 |
|---|---|---|
| 1 | `use <name>` 版本选择 | ✅ |
| 2–4 | `install` / `remove` / `update` 确认 | ✅ |
| 5–7 | `self install` 的三处确认 | ✅ |
| 8 | `install <模糊名>` 包名消歧 | ✅ |
| 9–13 | `config --lang/--mirror/--theme/--ui-mode/--interactive` 缺值 | ⏳ 本轮已实现,待发布 |

### 3.2 缺口的共同形状

用户报的 `xlings subos use` 是典型:

```
$ xlings subos use
  Sub-OS environments:
    agent-influence  (…)
    ...45 个...
```

**列出 45 个,然后要求用户把其中一个重打一遍** —— 而且这段列表还没有 i18n。凡是这个形状的地方,交互都该覆盖。

### 3.3 建议覆盖到 31 处

| # | 决策点 | 候选来源 | 现状 | 优先级 |
|---|---|---|---|---|
| 1 | `use <name>` 版本 | 当前 subos 已装版本 | ✅ | — |
| 2–4 | install / remove / update 确认 | y/n | ✅ | — |
| 5–7 | `self install` 三处确认 | y/n | ✅ | — |
| 8 | `install <模糊名>` 消歧 | 模糊匹配前 5 | ✅ | — |
| 9–13 | `config` 五个枚举设置缺值 | 各自枚举 | ⏳ | — |
| **14** | **`subos use` 无参** | 全部 subos | ❌ | **高**(用户已报) |
| **15** | `subos remove` 无参 | 全部 subos | ❌ | 高(破坏性,更需要看清再选) |
| **16** | `subos info` 无参 | 全部 subos | ❌ | 中 |
| **17** | `use <name>` 的 shim 版本(`no_active_version`) | 已 opt-in 的版本 | ❌ | 高 |
| **18** | `remove <name>` 有多个 subos 引用时 | 引用它的 subos | ❌ | 中 |
| **19** | `info <name>` 名字模糊时 | 模糊匹配 | ❌ | 中 |
| **20** | `why <pkg> <dep>` 的 dep 缺省 | 该包的依赖 | ❌ | 低 |
| **21** | `update <pkg>` 无参 | 可更新的包 | ❌ | 中 |
| **22** | `subos new --from` 无值 | 全部 subos | ❌ | 低 |
| **23** | `subos new --storage` 无值 | shared/tmpfs/image | ❌ | 低 |
| **24** | `subos new --runtime` 无值 | 已装 runtime | ❌ | 低 |
| **25** | `list <filter>` 无匹配时 | 近似名 | ❌ | 低 |
| **26** | `search` 结果直接选中安装 | 搜索结果 | ❌ | 中 |
| **27** | `config --index-repo` 移除哪一个 | 已配置的 repo | ❌ | 低 |
| **28** | `self doctor --fix` 逐项确认 | 每个 finding | ❌ | 中 |
| **29** | `remove` 有依赖者时的 `--force` 确认 | y/n | ❌ | 中 |
| **30** | `install` 跨 subos 时选目标 subos | 全部 subos | ❌ | 低 |
| **31** | 首次运行的引导(语言/镜像) | 各自枚举 | ❌ | **不做,见下** |

**第 31 项建议不做**,理由与之前一致:首次运行时用户还没有判断依据,探测优于提问。列出来是为了说明它被考虑过并被否决,而不是遗漏。

**如果实施时发现某项不成立,必须在实施记录里写明为什么**,而不是静默从表里去掉 —— 这份表是承诺,不是愿望。

### 3.4 三条约束(不变)

1. 交互只能替换"重打一遍",不能是唯一路径
2. 回落必须是完整答案,不是错误
3. 确认层看 `stdin` 且脱离 `tui.interactive`;选择层额外要求它

### 3.5 交互提示语必须走 i18n

现有 8 处提示语全是硬编码英文(`Which {} ?` / `Multiple matches found. Select a package:` / `Remove {} from subos '{}' ?`)。扩到 31 处会变成 31 条硬编码英文。

**因此 §2 的 i18n 接线必须先于或同步于 §3 的扩展**,否则每加一个交互点就多一条要回头翻译的字符串。提示语的 msgid 用 `PromptEvent::id`(已存在、稳定、唯一),与诊断用 `diag.code` 同构。

---

## 4. 选择器重做

### 4.1 现状与问题

```
╭────────────────────────────────────────────╮
│ Which mirror ?                             │
├────────────────────────────────────────────┤
│ > CN                                       │
├────────────────────────────────────────────┤
│ ↑↓ navigate  Enter select  Esc cancel      │
╰────────────────────────────────────────────╯
```

两个问题:

**其一,配色。** 六处边框全部使用 `theme::border()`,深色主题下是 `#334155`(slate-700)。在深色背景(`#0D1117`)上对比度约 **1.6:1**,远低于可读下限。边框、分隔线、提示行都是这个颜色,于是整个框"糊"在背景里。

**其二,形态。** 一个带圆角边框、上下分隔线、底部提示行的**盒子**,为了从 2 个选项里选 1 个要占 6 行。它与 xlings 其余输出(左对齐、无框、`◆`/`▸` 前缀)也不是一套视觉语言。

### 4.2 形态方案 —— 需要你裁决

**建议:内联列表,无框。**

```
  ◆ Which mirror ?
  ▸ CN                          (current)
    GLOBAL

    ↑↓ move   enter select   esc cancel
```

- 与 `xlings subos` / `xlings list` 的既有输出同一套语言(`◆` 标题、`▸` 当前项)
- 2 个选项占 5 行而不是 6;10 个选项占 13 行而不是 14,且没有边框宽度限制
- 选中项用 `accent` 而非反色块;当前值用 `muted` 标注 `(current)`
- 底部提示行保留但降为 `muted`,且**只在首次出现**(复用已有的 `hint_seen` 机制)

**配色修正**(与形态无关,两案都要):

| 用途 | 现在 | 改为 | 理由 |
|---|---|---|---|
| 选中项 | 反色块 | `accent` 前景 + `▸` | 反色块在窄终端里会撑满整行,视觉噪声大 |
| 未选项 | `text` | `text` | 不变 |
| 提示行 | `border` | `muted` | `muted` 深色下是 `#94A3B8`,对比度约 7:1 |
| 分隔/边框 | `border` | 取消 | 无框则不需要 |

`border` 槽本身**不改** —— 它在面板场景下是合适的;问题是选择器把它用在了需要可读性的地方。

**备选案(若你更想保留框):** 保留 `borderRounded`,但边框改用 `muted`,提示行改用 `muted`,选中项改 `accent`。改动更小,视觉语言仍与其余输出不一致。

### 4.2b 形态归主题所有,不是一个全局开关

三种形态,内置的三个主题各用一个 —— 这不是为了给选项而给选项:**一个为单色终端做的配色和一个为投影仪做的配色,对"要不要装饰"的答案本来就不同**,强行共用一个控件必然有一个是错的。

| 主题 | 形态 | 为什么是它 |
|---|---|---|
| `default` | `inline` | `◆` 标题 + `▸` 选中 + muted 提示行。与 `xlings subos` / `xlings list` / 每条诊断同一套视觉语言 |
| `mono` | `plain` | 无图标、无强调色,`>` 标记 + 加粗。图标与颜色正是单色终端和色觉障碍读者用不上的东西,而"不装饰"就是这个主题存在的理由 |
| `high-contrast` | `framed` | `accent` 画的重边框,选中项反色。阳光下或投影仪上,边界正是让控件可读的东西;它在别处带来的噪声,是这个主题愿意付的代价 |

实际渲染:

```
default                        mono                        high-contrast
                                                           ┏━━━━━━━━━━━━━━━━━━━┓
  ◆ Which UI mode ?              Which UI mode ?            ┃ Which UI mode ?   ┃
                                                           ┠───────────────────┨
    auto                           auto                    ┃  auto             ┃
  ▸ cli        (current)         > cli        (current)     ┃  cli    (current) ┃
    tui                            tui                      ┃  tui              ┃
                                                           ┗━━━━━━━━━━━━━━━━━━━┛
    ↑↓ …                           ↑↓ …
```

**关键一点:`framed` 的边框用 `accent`,不是 `border`。** 「灰色太暗」的根因是拿 `border`(`#334155`,1.6:1)去画需要可读性的东西;`framed` 保留了框但换了槽位,所以它是高对比,而不是重蹈覆辙。`border` 槽本身仍然只用于面板里的分隔线,那里低对比是对的。

**字段与其他槽同一套语义**:主题文件里写 `"selector": "plain"`,不写则继承内置的 `inline`;写了未知值会被**报告**(`LoadIssue::UnknownSlot`,附上 `inline, plain 或 framed`)而不是静默回退 —— 否则"设了没反应"和"设了但没生效"又分不清,那正是这套加载器存在的原因。

它经 `ui::theme::selector()` 转发给前端,前端不直接触碰 `xlings::theme` —— 装饰和颜色是同一类问题,走同一扇门。

### 4.3 验证

选择器是 TUI,自动化断言只能覆盖行为不能覆盖观感。可测的部分:

1. pty 下方向键 + 回车 → 选中项被写入(已有 N9 的模式)
2. `esc` → 取消,退出码 0,状态不变
3. 非交互 → 完全不渲染,回落到列表
4. `XLINGS_TERM_WIDTH=40` 下不换行错乱
5. **对比度**:取渲染后的 SGR 前景色与背景色,计算 WCAG 对比度并断言 ≥ 4.5:1 —— 这条能自动化,且正是现在会失败的那条

---

## 5. 任务与依赖

| # | 任务 | 依赖 | 验证 |
|---|---|---|---|
| P1 | `coordinate_from_payload_path` 零分配 | — | 新旧输出逐字节比对 + 计时 |
| P2 | `info` 走按 target 过滤的窄路径 | P1 | 同上,且 `list`/`doctor`/`remove` 输出不变 |
| N1 | 合并 `spec.cpp` 与 `cli.cpp` 的描述为一处 | — | cli spec parity 检查仍绿 |
| N2 | 子命令 / 选项 / 参数描述接 i18n(~110) | N1 | `--lang zh` 下 `subos -h` 全中文,命令名仍英文 |
| N3 | 面板表头接 i18n(~12) | N1 | `subos use` 的列表标题为中文 |
| N4 | 交互提示语接 i18n(8→31) | N1 | 提示语随 `--lang` 变化 |
| I1 | 选择器改内联无框 + 配色 | — | §4.3 五条 |
| I2 | 交互覆盖 14–17(高优先) | I1, N4 | 各自 e2e:非交互回落完整 |
| I3 | 交互覆盖 18–21, 26, 28–29(中) | I2 | 同上 |
| I4 | 交互覆盖 22–25, 27, 30(低) | I3 | 同上 |

建议切分:**P1+P2 单独一版**(纯性能,输出不变,风险独立);**N1–N4 + I1 一版**;**I2–I4 一版**。

---

## 6. 当前状态

方案待 review。§4.2 的形态取舍需要你定;其余为缺陷修复与已确认要求。

---

## 7. 实施记录

### 7.1 已落地

| | 内容 | 结果 |
|---|---|---|
| **P1** | `coordinate_from_payload_path` 零分配 | `info gcc` 2.40s → **1.26s**;输出逐字节相同(5 组比对) |
| **I1** | 选择器改内联无框 + 配色 | 见 §4.2;`tests/scripts/test_selector_contrast.py` 已入 CI |
| **T20** | `config` 五个设置缺值时进入选择 | 有值不变;无值时交互开→选择器,关→列出取值 exit 2 |
| **N2** | `spec.cpp` 的 103 条描述接 i18n | `xlings subos -h` 全中文,旗标/占位符保持英文 |

### 7.2 方案中被实施推翻的两处

**N1(合并 `spec.cpp` 与 `cli.cpp` 的描述)不必做。** 方案假设 parity 检查要求两份描述一致,实际它**只比对旗标**(实测 271 个拼写),描述可以不同。而且用户看到的帮助全部来自 `spec.cpp`(`spec::find()` → `print_subcommand_help`),`cli.cpp` 的 49 处 `.description()` 只喂给 cmdline 库自身的 usage,从不显示。所以只翻 `spec.cpp` 即可,零合并成本。

**键不用新造,英文原文即是键。** 方案设想给 103 条各配一个 `cmd.*.desc` 形式的 ID,那需要同时改 `spec.cpp` 和 `en.cppm` 两处 206 行。改用英文原文作键后:`spec.cpp` **一个字未改**,`en.cppm` 无需新增(`tr()` 找不到就返回键,而键就是英文原文),只有 `zh.cppm` 增量补 103 条。这与 §2.4 给标签定的规则本来就一致,方案里没把它推广到描述上是遗漏。

### 7.3 写测试时发现的缺陷(与 i18n 无关)

`xlings subos use -h` 的标题与用法行显示的是 **`xlings use`** —— 丢了 `subos` 这一层,而 `xlings use` 是一条真实存在、语义完全不同的命令(切换工具版本)。照抄那一行的人会跑错命令。

根因:`print_subcommand_help(command->name, …)` 传的是叶子名,而调用处本来就持有完整的 `commandPath`。

它是被 `test_i18n_coverage.py` 里那条"用户会照抄的字面量必须原样保留"的断言抓到的 —— 断言的对象选对了,就会顺带抓到与它本来目的无关的问题。

### 7.4 未做

| | 为什么 |
|---|---|
| **P2** `info` 的剩余 1.26s | 需要把标识符 intern 成整数索引,是动 `list`/`doctor`/`remove` 共用路径的结构性改动,单独立项 |
| **I2–I4** 交互扩到 31 处 | §3.3 的 14–30 项;本轮先把选择器形态与 `config` 五项做定,形态定了再批量接入 |
| **N3** 面板表头 i18n | `subos use` 的列表标题等约 12 处 |
| **N4** 交互提示语 i18n | 选择器的两条已接(`ui.select_keys` / `ui.select_package`),其余 6 处待接 |

**i18n 当前条目数:144(zh),覆盖底线 100 已由 `test_i18n_coverage.py` 钉住。**
